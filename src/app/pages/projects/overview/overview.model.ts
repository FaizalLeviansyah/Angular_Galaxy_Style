export interface projectListModel {
  icon?: string;
  icon_color?: string;
  name?: string;
  type?: string;
  size?: string;
  date?: string;
}

export interface TeamModel {
  id: string;
  image: string;
  bg_color: string;
  name: string;
  position: string;
  projects: string;
  tasks: string;
  star: string;
}
